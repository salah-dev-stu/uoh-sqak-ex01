"""Sinusoid Extractor — FC / RNN / LSTM extractors for a noisy 4-sine mixture.

HW1 deliverable for course 203.3763 (University of Haifa). The public surface
is intentionally tiny: only the SDK class is exported. Internal services and
models are reachable for tests but discouraged for downstream consumers.
"""

from sinusoid_extractor.shared.version import __version__

__all__ = ["__version__"]
